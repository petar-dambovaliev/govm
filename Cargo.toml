[package]
name = "gno-rs"
version = "0.1.0"
edition = "2021"

[workspace]
members = [
    "parser",
    "vm"
]

[dependencies]
vm = { path = "./vm" }
parser = { path = "./parser" }

[profile.release]
lto = true
opt-level = 3
codegen-units = 1